# Fashion_Bot Python dependencies
#
# This repository has two “tracks”. You can install everything with:
#   pip install -r requirements.txt
#
# If you only work on one track, you may not need the other section — but keeping
# them together makes onboarding and CI simpler.

# ============================================================
# Track B — Recommender / API / Eval (main app)
# ============================================================

# --- API & app ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
python-multipart>=0.0.27

# --- Recommender: catalog & scoring ---
pandas>=2.2.0
numpy>=1.26.0

# --- Postgres + pgvector (dense retrieval & embedding storage) ---
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0

# --- OpenAI (query parser, reranker, embeddings API) ---
openai>=1.40.0


# ============================================================
# Track A — QA / RAG (teammate scripts)
# Used by: src/qa/scripts/web_scraping.py, process_for_rag.py, build_db.py, query_answer.py
# Settings required:
# - OPENAI_API_KEY must be set (in .env or environment) for query answering via LangChain ChatOpenAI.
# Notes:
# - First run downloads the embedding model (all-MiniLM-L6-v2).
# ============================================================

# --- Scraping ---
requests>=2.33.0
beautifulsoup4>=4.12.0

# --- Vector DB + LangChain ---
chromadb>=1.5.0
langchain>=1.2.0
langchain-core>=1.3.0
langchain-chroma>=1.1.0
langchain-openai>=1.2.0
langchain-huggingface>=1.2.0

# --- Embeddings backend used by HuggingFaceEmbeddings ---
sentence-transformers>=5.4.0
# required by langchain-openai
tiktoken>=0.7.0


# ============================================================
# Dev / tests
# ============================================================
pytest>=8.3.0
