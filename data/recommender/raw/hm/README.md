# H&M Raw Data

Place raw H&M assets here:

- `articles.csv`
- product images under `images/`

For now, the repository also contains a top-level `articles.csv`. You can either:

1. keep it there temporarily and read from that path, or
2. move it into this folder once the ingestion pipeline is ready
